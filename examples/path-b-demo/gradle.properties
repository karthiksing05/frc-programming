# GradleRIO + AdvantageKit
org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

# Pinned versions — DO NOT bump without re-testing all lessons.
# AdvantageKit explicitly says forward-compat across seasons is not
# guaranteed; see docs.advantagekit.org/whats-new.
wpiVersion=2026.2.1
advantageKitVersion=26.0.2

# Set this if a school network blocks maven central / frcmaven
# systemProp.http.proxyHost=...
